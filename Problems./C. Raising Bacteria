#include <iostream>
using namespace std;

int main() {
    int x;
    cin >> x;

    int count = 0;
    while (x > 0) {
        count += (x & 1);  // Add 1 if the last bit is 1
        x >>= 1;           // Right shift (divide by 2)
    }

    cout << count << endl;
    return 0;
}
